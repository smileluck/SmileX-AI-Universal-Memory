"""_shared — locomo / longmemeval / mem0_compat 共用的 LLM 接入件.

2026-09 由两目录逐字节相同的副本提公;脚本经 benchmarks/ 目录引导导入:
    sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
    from _shared.llm_client import get_client
"""
