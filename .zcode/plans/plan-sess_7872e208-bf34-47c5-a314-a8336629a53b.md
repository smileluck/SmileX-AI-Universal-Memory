BM25+FTS5 关键词双通道检索（对标 mem0 双通道标配，修复精确词漏检）：

1. schema/013_fts5.sql + SCHEMA_VERSION 13：FTS5 external-content 虚拟表 fts_fragments(trigram tokenizer)，temporal_fragments 上的 INSERT/UPDATE/DELETE 触发器自动同步（覆盖全部 5 个写入点与归档删除）
2. storage/queries/fts.py（新）：bm25_fragment_search —— 问句分词（丢弃 <3 字符）OR 连接，bm25 排序 + scope 后过滤
3. ContextBuilder._collect L1 通道：向量 KNN rank + BM25 rank → RRF 融合（k=60），归一化到 [0,1]，纯关键词命中也进候选
4. tests/unit/test_fts.py：触发器同步/端到端关键词召回/中文/归档移除/RRF 分数；回归全量单测
5. 用 benchmark #9 题本地验证 gold_in_context False→True（无需 LLM），再抽查无回归

不动 MemoryMiddleware API、knn_search、L2 hybrid；reranker 留作下一步。